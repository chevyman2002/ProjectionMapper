The quad mesh issues have been resolved. However, I imported the same video twice, created Mesh 1 and sent it to projector output 1. I then created meshes 2 and 3, sending them to the projector output 2. What I see is that the output for all mesh layers are being sent to both projectors, instead of just the one that was selected for the respective source videos. I need the outputs to explicitly display only the source video(s) configured to use them, and nothing else.